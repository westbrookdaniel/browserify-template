{
  "name": "browserify-template",
  "version": "1.1.0",
  "description": "A template for getting started with browserify quickly.",
  "scripts": {
    "build": "browserify src -p esmify > public/bundle.min.js",
    "start": "budo src:public/bundle.min.js --live -- -p esmify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "budo": "^11.6.4",
    "browserify": "^16.5.2",
    "esmify": "^2.1.1"
  }
}
